node_modules/
.expo/
dist/
web-build/
# Managed Expo workflow — generated by `expo prebuild` / a native-module
# `expo install` step (e.g. react-native-purchases). Don't commit these.
/ios/
/android/
*.jks
*.p8
*.p12
*.key
*.mobileprovision
*.orig.*
.DS_Store
.env*.local
