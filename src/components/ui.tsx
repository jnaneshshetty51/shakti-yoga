"use client";

/**
 * App-wide design-system primitives. Canonical source lives in `./admin/ui`
 * (first built for the admin panel); re-exported here so member, teacher and
 * marketing screens can import from a neutral path.
 */
export * from "./admin/ui";
